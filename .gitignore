# OS
.DS_Store
Thumbs.db
.~lock.*

# IDE
.vscode/
.idea/
*.swp

# Node
node_modules/
.npm
npm-debug.log*

# Expo / RN build output
mobile/.expo/
mobile/dist/
mobile/web-build/
mobile/android/build/
mobile/android/app/build/
mobile/android/.gradle/
mobile/ios/build/
mobile/ios/Pods/
mobile/*.apk
mobile/*.aab

# Native
*.jks
*.keystore
!mobile/android/app/debug.keystore

# Claude Code transient state — should never be committed
.claude/worktrees/
mobile/.claude/worktrees/
mobile/.validate-tmp*/
mobile/.validate-seed.js

# Scratch/test detritus patterns (R-014 incidents left these behind)
mobile/_test/
**/_truncated_test.*

# ATO / financial — local only
*.xlsx
*.xls

# Test artefacts
coverage/
*.lcov

# Misc
.env*
!.env.example

# Sandbox scratch dirs created by Claude during local testing
mobile-helpers-test/
/db/
.icon-gen/

# Agentic usability tester output (reports + screenshots)
tester/output/
