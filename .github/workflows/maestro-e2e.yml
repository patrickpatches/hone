# Maestro E2E — Phase 2 stub (NOT YET ACTIVE)
#
# This workflow is a placeholder. Uncomment the real job below once the
# Maestro suite has 10+ stable flows and you're ready to run it in CI.
#
# Prerequisites before enabling the real job:
#   1. Suite must be stable — no flaky flows.
#   2. APK artifact retention must cover the gap between eas-build and this run.
#   3. Uses API 26 emulator (matches mobile/android minSdkVersion 26).
#
# To enable: delete the stub job below and uncomment the e2e job block.

name: Maestro E2E (stub — not active)

on:
  workflow_dispatch:
    inputs:
      note:
        description: 'Note — this workflow is a stub; the real E2E job is commented out below'
        required: false
        default: 'stub'

jobs:
  stub:
    name: Phase 2 not yet active
    runs-on: ubuntu-latest
    steps:
      - name: Not active yet
        run: |
          echo "Maestro E2E suite is Phase 2."
          echo "Enable when 10+ stable flows exist — see maestro/flows/ and HONE-016."
          echo "To wire in: uncomment the real job in this file."

# ── Real E2E job (uncomment when ready) ───────────────────────────────────────
#
# jobs:
#   e2e:
#     name: Maestro smoke suite
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4
#
#       - name: Download APK artifact
#         uses: dawidd6/action-download-artifact@v6
#         with:
#           run_id: ${{ github.event.inputs.run_id }}
#           name_is_regexp: true
#           name: tuckerspice-v.*
#           path: apk/
#
#       - name: Locate APK
#         id: apk
#         run: |
#           APK=$(find apk/ -name "*.apk" | head -1)
#           echo "path=$APK" >> "$GITHUB_OUTPUT"
#
#       - name: Enable KVM
#         run: |
#           echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' \
#             | sudo tee /etc/udev/rules.d/99-kvm4all.rules
#           sudo udevadm control --reload-rules
#           sudo udevadm trigger --name-match=kvm
#
#       - name: Install Maestro CLI
#         run: curl -Ls "https://get.maestro.mobile.dev" | bash
#
#       - name: Run Maestro suite on emulator
#         uses: reactivecircus/android-emulator-runner@v2
#         with:
#           api-level: 26
#           arch: x86_64
#           profile: pixel_6
#           force-avd-creation: false
#           emulator-options: -no-window -gpu swiftshader_indirect -noaudio -no-boot-anim
#           disable-animations: true
#           script: |
#             adb install "${{ steps.apk.outputs.path }}"
#             ~/.maestro/bin/maestro test maestro/flows/
